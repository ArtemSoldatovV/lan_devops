name: Version update pipeline

on:
  pull_request:
    types: [closed]
    branches:
    - release

env:
  NAME_VEPSION_FILE: version
jobs:
  validate-branch:
    runs-on: ubuntu-latest
    steps:
      - name: parse data
        id: parse-data
        run: tj-actions/branch-names@v8

      - name: Print info about actions
        id: repo-info
        run: |
          echo "EVENT NAME    : ${{ github.event_name }}"
          echo "ACTION        : ${{ github.event.action }}"
          echo "EVENT NAME    : ${{ github.event_name }}"
          echo "IS            : ${{ github.event.pull_request.merged}}"
          echo "CURRENT BRANCH: ${{ steps.branch-names.outputs.current_branch }}"
          echo "REF           : ${{ github.ref }}"
          echo "REF NAME      : ${{ github.ref_name }}"
          echo "DEFAULT BRANCH: ${{ steps.branch-names.outputs.default_branch }}"
       
 #     - name: validate branch
 #       id: validate_branch
 #       if: |
 #         contains(steps.branch-names.outputs.current_branch, 'feature') ||
 #         contains(steps.branch-names.outputs.current_branch, 'hotfix')
 #       run: echo "Branh [feature of hotfix] is valid! "
 #       if: |
 #         contains(steps.branch-names.outputs.current_branch, 'feature') == fales &&
 #         contains(steps.branch-names.outputs.current_branch, 'hotfix') == fales
 #       run: exit 

  print-data-information:
    runs-on: abuntu-latest
    steps:
      - name: Second step
        run: echo "Print info"
      - name: 3 step
        run: echo "Print info"
